export const dynamic = "force-dynamic";
export const revalidate = 60;

export default function Page() {
  return (
    <div style={{padding: 24}}>
      <h1>AGI</h1>
      <p>Minimal placeholder – build OK. (Do të rikthejmë komponentët pas suksesit.)</p>
    </div>
  );
}
