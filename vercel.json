{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "corepack enable && yarn build",
  "installCommand": "corepack enable && yarn install --immutable || yarn install",
  "functions": {
    "api/**/*.ts": { "runtime": "nodejs20.x" },
    "api/**/*.js": { "runtime": "nodejs20.x" }
  }
}
